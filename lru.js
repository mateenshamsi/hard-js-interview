class LRUCache{
    constructor (capacity){
        this.capacity = Number(capacity) 
        this.map =  new Map() 
        this.head = null 
        this.tail = null 
    }
    get(key){

    }
    put(key,value){ 
       if(this.map.has(key))
        { 
            
        }         
    }
}